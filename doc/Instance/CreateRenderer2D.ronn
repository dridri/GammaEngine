Instance::CreateRenderer2D(3) -- Create a new 2D Renderer attached to this instance
===================================================================================

## SYNOPSIS

`Renderer2D* CreateRenderer2D()`


## DESCRIPTION

This function asks the underlying backend to create a new 2D `Renderer2D`.

## RETURN VALUES

Returns a pointer to the newly created 2D `Renderer2D`
